// HayahAI SDK — Main entry point
// Types + Client + Theme (works in any JS environment)

export * from "./types";
export { HayahAIClient } from "./client";
export { defaultTheme, mergeTheme } from "./theme";
